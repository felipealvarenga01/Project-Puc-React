export type InputProperties = {
    type?: string,
    placeholder?: string,
    id?: string,
    text?: any
    textLabel?: string
}
export type LabelProperties = {
    htmlFor?: string,
    text?: string
}

export type InfosProperties = {
    dataNascimento: string,
    nome: string,
    sobreNome: string,
    email: string,
    uid: string
}
